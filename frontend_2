import React, { useState } from 'react';
import { 
  Users, 
  Clock, 
  Globe, 
  Code, 
  ChevronLeft, 
  CheckCircle2, 
  ArrowRight, 
  FileText, 
  Phone, 
  Video 
} from 'lucide-react';

export default function DigitalLeafletApplet() {
  const [selectedChallenge, setSelectedChallenge] = useState(null);

  // The Data State: Mapping Challenges to AI Solutions
  const workflows = [
    {
      id: 'volume',
      challenge: 'Overwhelming Volume of Resumes',
      icon: Users,
      solution: 'AI Resume Screener',
      solutionIcon: FileText,
      description: 'Instantly filter, analyze, and rank thousands of resumes. Our AI provides detailed insights against your specific job requirements, surfacing the top 1% of talent in seconds.',
      features: [
        'Contextual keyword & skill matching',
        'Automated skill gap analysis',
        'Bias-free filtering algorithms',
        'Instant shortlisting dashboards'
      ]
    },
    {
      id: 'tech',
      challenge: 'Time-Consuming Tech Assessments',
      icon: Code,
      solution: 'AI Coding Interviewer',
      solutionIcon: Code,
      description: 'Conduct live, adaptive coding assessments at scale. Evaluate technical skills and real-world problem-solving abilities without tying up your senior engineers.',
      features: [
        'Live IDE execution & compilation',
        'Adaptive difficulty scaling',
        'Advanced AI plagiarism detection',
        'Detailed code quality scoring'
      ]
    },
    {
      id: 'screening',
      challenge: 'Delays in Initial Phone Screens',
      icon: Clock,
      solution: 'AI Phone Screener',
      solutionIcon: Phone,
      description: 'Deploy conversational AI agents to automate preliminary candidate screenings. Evaluate baseline responses and enthusiasm to instantly identify the best cultural fits.',
      features: [
        'Natural Language Processing (NLP)',
        'Voice sentiment & tone analysis',
        'Automated follow-up scheduling',
        'Customizable interview scripts'
      ]
    },
    {
      id: 'language',
      challenge: 'Inconsistent Language Checks',
      icon: Globe,
      solution: 'English Proficiency Test',
      solutionIcon: Globe,
      description: 'Objectively assess candidates\' language skills, including fluency, grammar, pronunciation, and vocabulary, using advanced audio processing AI.',
      features: [
        'Strict CEFR standard alignment',
        'AI pronunciation & accent scoring',
        'Real-time fluency assessment',
        'Automated grammar correction analysis'
      ]
    }
  ];

  return (
    <div className="min-h-screen bg-slate-950 font-sans flex items-center justify-center p-4 sm:p-8 relative overflow-hidden text-slate-200">
      
      {/* Decorative Ambient Background Glows */}
      <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-emerald-600/10 rounded-full blur-[120px] pointer-events-none"></div>
      <div className="absolute bottom-1/4 right-1/4 w-96 h-96 bg-teal-600/10 rounded-full blur-[120px] pointer-events-none"></div>

      {/* Main Applet Container */}
      <div className="max-w-4xl w-full bg-slate-900/60 backdrop-blur-2xl border border-white/10 rounded-3xl shadow-2xl relative z-10 overflow-hidden min-h-[500px] flex flex-col">
        
        {/* Top Progress/Brand Bar */}
        <div className="h-1.5 w-full bg-slate-800">
          <div 
            className="h-full bg-gradient-to-r from-emerald-500 to-teal-400 transition-all duration-700 ease-out"
            style={{ width: selectedChallenge ? '100%' : '25%' }}
          ></div>
        </div>

        <div className="p-8 sm:p-12 flex-1 flex flex-col justify-center">
          
          {/* VIEW 1: Selection Grid */}
          {!selectedChallenge && (
            <div className="animate-in fade-in zoom-in-95 duration-500">
              <div className="text-center mb-12">
                <span className="text-emerald-400 font-semibold tracking-wider uppercase text-sm mb-3 block">Interactive Guide</span>
                <h2 className="text-4xl sm:text-5xl font-bold text-white tracking-tight">
                  What is your biggest <br /> <span className="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-teal-300">hiring bottleneck?</span>
                </h2>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6">
                {workflows.map((item) => {
                  const Icon = item.icon;
                  return (
                    <button
                      key={item.id}
                      onClick={() => setSelectedChallenge(item)}
                      className="group text-left bg-slate-800/40 hover:bg-slate-800 p-6 rounded-2xl border border-white/5 hover:border-emerald-500/40 transition-all duration-300 shadow-sm hover:shadow-emerald-500/10 flex items-center gap-5 focus:outline-none focus:ring-2 focus:ring-emerald-500/50"
                    >
                      <div className="w-14 h-14 shrink-0 flex items-center justify-center rounded-xl bg-slate-900 border border-white/5 group-hover:bg-emerald-500/10 group-hover:border-emerald-500/20 text-slate-400 group-hover:text-emerald-400 transition-colors">
                        <Icon size={24} />
                      </div>
                      <div>
                        <h3 className="text-lg font-semibold text-slate-200 group-hover:text-white transition-colors">{item.challenge}</h3>
                        <p className="text-slate-500 text-sm mt-1 flex items-center gap-1 group-hover:text-emerald-400/80 transition-colors">
                          Reveal solution <ArrowRight size={14} className="group-hover:translate-x-1 transition-transform" />
                        </p>
                      </div>
                    </button>
                  );
                })}
              </div>
            </div>
          )}

          {/* VIEW 2: Solution Reveal */}
          {selectedChallenge && (
            <div className="animate-in fade-in slide-in-from-right-8 duration-500 flex flex-col h-full">
              
              <button 
                onClick={() => setSelectedChallenge(null)}
                className="self-start flex items-center gap-2 text-sm font-medium text-slate-400 hover:text-white transition-colors mb-8 group"
              >
                <div className="p-1 rounded-full bg-slate-800 group-hover:bg-slate-700 transition-colors">
                  <ChevronLeft size={16} />
                </div>
                Explore another challenge
              </button>

              <div className="grid md:grid-cols-2 gap-12 items-center flex-1">
                {/* Left Side: Context */}
                <div>
                  <div className="w-16 h-16 flex items-center justify-center rounded-2xl bg-emerald-500/10 border border-emerald-500/20 text-emerald-400 mb-6 shadow-[0_0_30px_rgba(52,211,153,0.15)]">
                    <selectedChallenge.solutionIcon size={32} />
                  </div>
                  <h2 className="text-3xl font-bold text-white mb-4 leading-tight">
                    {selectedChallenge.solution}
                  </h2>
                  <p className="text-slate-400 text-lg leading-relaxed mb-8">
                    {selectedChallenge.description}
                  </p>
                  
                  <button className="px-8 py-3.5 bg-emerald-500 hover:bg-emerald-400 text-slate-950 font-bold rounded-xl shadow-[0_0_20px_rgba(52,211,153,0.2)] hover:shadow-[0_0_25px_rgba(52,211,153,0.4)] transition-all transform hover:-translate-y-0.5">
                    Book a Demo
                  </button>
                </div>

                {/* Right Side: Features Board */}
                <div className="bg-slate-950/50 rounded-2xl p-8 border border-white/5 relative">
                  <div className="absolute top-0 right-8 w-32 h-1 bg-gradient-to-r from-transparent via-emerald-500 to-transparent opacity-50"></div>
                  
                  <h3 className="text-sm font-bold tracking-widest text-slate-500 uppercase mb-6">Key Capabilities</h3>
                  
                  <ul className="space-y-5">
                    {selectedChallenge.features.map((feature, index) => (
                      <li key={index} className="flex items-start gap-4">
                        <CheckCircle2 size={20} className="text-emerald-400 shrink-0 mt-0.5" />
                        <span className="text-slate-300 font-medium">{feature}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
              
            </div>
          )}

        </div>
      </div>
    </div>
  );
}
